# Isolated Containers

**Isolated Containers** are specialized environments used for testing software within a **restricted, lightweight environment**.

If a container cannot fulfill the specific requirements of a task—such as needing a different OS kernel or deeper hardware access—you should use a **Virtual Machine (VM)** instead.
