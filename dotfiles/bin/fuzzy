#!/usr/bin/env bash

select_location(){
    local selected
    selected=$(find $HOME -mindepth 1 -maxdepth 4 -type d | fzf)
    echo "$selected"
}

select_location

# set alias to .zsh_profile
# ^f = "$(cd $(fuzzy))"
