#!/usr/bin/env bash

select_location(){
    local selected
    selected=$(find $HOME -mindepth 1 -maxdepth 4 -type d | fzf +m --height 50% --style full --input-label ' Destination ' --preview 'tree -C {}')
    echo "$selected"
}

select_location

# set alias to .zsh_profile
# ^f = "$(cd $(fuzzy))"
