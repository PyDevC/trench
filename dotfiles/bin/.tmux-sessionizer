#!/usr/bin/env bash

if [[ -z "$(pwd | grep $HOME/personal)" ]] || [[ "$(pwd)" == "$HOME/personal" ]]; then
    if [[ -z "$(pwd | grep $HOME/work)" ]] || [[ "$(pwd)" == "$HOME/work" ]]; then
        clear
        return
    fi
fi

tmux new-window -dn scratch
clear
nvim .
